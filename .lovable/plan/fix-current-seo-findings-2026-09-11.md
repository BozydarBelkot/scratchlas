# Fix current SEO findings

## Changes
- Add the published sitemap URL to `robots.txt` while preserving all crawler rules.
- Update the sitemap generator to emit absolute HTTPS URLs on `scratchlas.lovable.app`.

## Verification
- Check the rendered `robots.txt` and `sitemap.xml` responses.
- Mark both SEO findings fixed so the next review can re-verify them.
